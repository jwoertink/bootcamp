#!/usr/bin/ruby
# -*- coding: utf-8 -*-

require 'rubygems'
if File.exist?('Gemfile')
  require 'bundler'
  Bundler.setup(:default)
end
$:.unshift(File.join(File.dirname(__FILE__), "..", "lib"))
require 'bootcamp'


Bootcamp::Worker.start # Get the work started!